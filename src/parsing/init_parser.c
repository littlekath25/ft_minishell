
#include "../includes/shell.h"

t_tree	*init_parser(char *buffer)
{
	t_tree	*trees;
	t_tree	*current;

	trees = ft_calloc(1, sizeof(t_tree));
	if (!trees)
		return (false);
	while (*buffer)
	{
	}
	return (1);
}
